/**
 * State the command indicator in the 3d view can have
 */
export enum IndicatorState {
  LANE_CHANGE_LEFT,
  LANE_CHANGE_RIGHT,
  TURN_LEFT,
  TURN_RIGHT,
  INCREASE_VELOCITY,
  DECREASE_VELOCITY,
  NONE,
}
